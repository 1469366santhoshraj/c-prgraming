#include<stdio.h>
int main()
{float a,b,c; 
int d;
printf("enter the two floating point");
scanf("%f %f",&a,&b);
c=a*b;
d=a*b;
printf("the product of the floating points in decimal is %f in integer %d",c,d);
return 0;
}
